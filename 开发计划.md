# Role
资深 Python 图像处理工程师

# Task
编写一个自动化 Python 脚本，将文件夹内的 6 张立方体贴图（Cubemap）按照 4x3 横向十字布局（Horizontal Cross）拼接为一张 UV 贴图。

# Input Specification
- 接收参数：`folder_paths` (List[str])，即一个或多个待处理的文件夹路径。

# Workflow & Logic
1. **文件扫描**：
   - 遍历每个文件夹，读取 `top`, `left`, `front`, `right`, `back`, `bottom` 6张散图。
   - 兼容扩展名：`.png`, `.jpg`, `.jpeg`。如果缺失任意一张，跳过该文件夹并报错。

2. **尺寸归一化 (Normalization)**：
   - 获取 6 张图的尺寸。若不一致，找出最大的 `(width, height)` 作为目标尺寸。
   - 使用 `Image.Resampling.LANCZOS` 将所有不符合目标尺寸的图片进行高质量缩放。

3. **画布拼装 (Composite)**：
   - 创建大小为 `(width * 4, height * 3)` 的透明画布（`mode="RGBA"`）。
   - 严格按照以下网格坐标 `(col, row)` 粘贴图片，且必须传入大图自身作为 mask 以保留透明度：
     * top -> (1, 0)
     * left -> (0, 1)
     * front -> (1, 1)
     * right -> (2, 1)
     * back -> (3, 1)
     * bottom -> (1, 2)

4. **保存与日志 (Output & Log)**：
   - 在当前文件夹下导出为 `uv_out.png`。
   - 打印清晰的控制台日志（如：成功提示或跳过原因）。

# Output Requirement
请直接给出完整、可运行、包含异常处理（Try-Except）的 Python 代码。